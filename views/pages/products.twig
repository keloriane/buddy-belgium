{% extends getf('base') %}
{% block body %}


  <div id="products" class="grey">
    <div class="vrac-product">
      <div class="title-container">
        <h2>Nos produits en vrac</h2>

      </div>

      <div class="product-list">
        <ul class="marques__scroll-menu__list">
          {% for product in vrac %}
            <li class="marques__scroll-menu__list__link">
              <a href="{{ site.url }}/pro/products/{{ product.slug }}">
                <img src={{ Image(product.product_image).src }}>
                <p>
                  {{ product.name }}
                </p>
                <p class="blue">
                  {{ product.category }}
                </p>


              </a>
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
    <div class="vrac-product">
      <div class="title-container">
        <h2>Nos produits emballé</h2>
      </div>
      <div class="product-list">
        <ul class="marques__scroll-menu__list">
          {% for product in paquet %}
            <li class="marques__scroll-menu__list__link">
              <a href="{{ site.url }}/pro/products/{{ product.slug }}">
                <img src={{ Image(product.product_image).src }}>

                 <p>
                   {{ product.name }}
                 </p>


              </a>

            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </div>

{% endblock %}
